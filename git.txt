git commands:
Branch: main
1. First, Make a Commit:
git commit -m "Inital commit"
2. Then, Push it to the main branch:
git push origin main
3. Extras: (Optional case):
git push -u origin main ( Push and set-up upstream)

[PS: Use terminal only] *if in VS-Code

